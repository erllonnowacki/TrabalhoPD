import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor:"#fff",
    paddingTop: 20,
    alignContent: "center"
 },
 title: {
  width: "100%",
  alignContent: 'center',
  textAlign: "center",
  fontSize: 24,
  fontWeight: "bold"
 }
 
});

export default styles